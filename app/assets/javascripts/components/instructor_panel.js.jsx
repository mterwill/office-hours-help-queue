var InstructorPanel = React.createClass({
  render: function () {
    return false;
    // return (
      // <h4 class="ui header">Instructor Queue Management</h4>

      // <Action title="Queue Pop" onClick={this.popQueue} className="ui huge bottom padded fluid primary button" />

      // var instructorStatusToggleMessage = this.

      // <Action title={instructorStatusToggleMessage} onClick={this.toggleInstructorStatus} className="ui large fluid bottom padded button" />
    // );
  },
  popQueue: function () {

  },
  toggleInstructorStatus: function () {

  }
});
